version: "3.8"

services:
  api:
    build: .
    ports:
      - "9000:9000"
    volumes:
      - .:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - pineapple_network

networks:
  pineapple_network:
    driver: bridge
